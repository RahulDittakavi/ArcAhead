# Copy to .env. Matches docker-compose.yml credentials.
DATABASE_URL="postgresql://arcahead:arcahead@localhost:5433/arcahead?schema=public"
PORT=4000
# The seeded demo user (single-user model for this build).
DEMO_USER_EMAIL="navigator@arcahead.app"
