// Re-export the shared API contract so screens import from one place.
export type {
  ArcStatus,
  ArcDto,
  JourneyDto,
  CharacterDto,
  LockedFactDto,
  ReactionDto,
  MeDto,
  SeriesRecord,
} from "@arcahead/shared";
